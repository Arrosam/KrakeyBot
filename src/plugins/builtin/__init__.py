"""Builtin plugin root — shipped with Krakey.

Loader scans this directory as if it were `workspace/tentacles/` or
`workspace/sensories/`, so the factory contract is identical. Nothing
special lives here beyond the plugins themselves.
"""
