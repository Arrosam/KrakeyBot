# workspace/sensories/

Drop sensory plugins here. On every boot Krakey scans this folder
and auto-registers valid plugins alongside the built-ins.

See [`PLUGINS.md`](../../PLUGINS.md) at the repo root for the contract,
manifest format, and a copy-pasteable example.

Anything in this directory (except this README) is gitignored by
default — your plugins are local.
