# Local config holds API keys — never commit.
config.yaml

.venv/
.claude/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
*.egg-info/
workspace/*
!workspace/GENESIS.md
!workspace/plugins/
# Within the plugins dir: user-dropped modules are local, keep only the stub
workspace/plugins/*
!workspace/plugins/README.md
*.sqlite
*.sqlite-journal
.env

# modular-dev tracking artifacts — local-only bus-agent helpers,
# regenerated from the codebase; never committed.
/graph.json
/overviews
/contracts/
/shared/
.claude/modular-dev-state.json
.claude/modular-dev-queue.json
# CLAUDE.md carries the local bus-agent workflow section; kept
# local-only at the user's request.
/CLAUDE.md
